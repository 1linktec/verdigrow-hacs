{
  "config": {
    "step": {
      "user": {
        "title": "Connect to VerdiGrow",
        "description": "VerdiGrow URL (e.g. http://192.168.1.7:8000 direct, or your https proxy URL) and the API token from VerdiGrow → Garden Setup → API access. Uncheck Verify SSL for a self-signed/untrusted https cert.",
        "data": {
          "url": "VerdiGrow URL",
          "token": "API token",
          "verify_ssl": "Verify SSL certificate (uncheck for self-signed / untrusted proxy certs)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect: {detail}",
      "invalid_auth": "Token rejected: {detail}"
    },
    "abort": {
      "already_configured": "This VerdiGrow instance is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "VerdiGrow — update frequency",
        "description": "Map your HA sensors to VerdiGrow metrics in VerdiGrow's console (Sensor Mapping). This integration pulls that map and pushes readings on the interval below. Default is once an hour — don't set it too low or you'll flood the database.",
        "data": {
          "update_interval": "Seconds between pushes"
        }
      }
    }
  }
}
