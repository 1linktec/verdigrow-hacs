{
  "config": {
    "step": {
      "user": {
        "title": "Connect to VerdiGrow",
        "description": "VerdiGrow URL (e.g. http://192.168.1.7:8000 direct, or your https proxy URL) and the API token from VerdiGrow \u2192 Garden Setup \u2192 API access. Uncheck Verify SSL for a self-signed/untrusted https cert.",
        "data": {
          "url": "VerdiGrow URL",
          "token": "API token",
          "verify_ssl": "Verify SSL certificate (uncheck for self-signed / untrusted proxy certs)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect: {detail}",
      "invalid_auth": "Token rejected: {detail}"
    },
    "abort": {
      "already_configured": "This VerdiGrow instance is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "VerdiGrow options",
        "menu_options": {
          "map_area": "Link an HA area to VerdiGrow",
          "add_mapping": "Add a sensor mapping",
          "remove_mapping": "Remove sensor mappings",
          "interval": "Update frequency"
        }
      },
      "interval": {
        "title": "Update frequency",
        "data": {
          "update_interval": "Seconds between pushes"
        }
      },
      "add_mapping": {
        "title": "Map an HA sensor to VerdiGrow",
        "description": "Pick an HA sensor, one or more VerdiGrow containers/areas it measures, and which metric it is.",
        "data": {
          "entity_id": "HA sensor",
          "metric": "Metric",
          "targets": "VerdiGrow targets (choose one or more)"
        }
      },
      "remove_mapping": {
        "title": "Remove mappings",
        "data": {
          "remove": "Select mappings to remove"
        }
      },
      "map_area": {
        "title": "Link an HA area to VerdiGrow",
        "description": "Pick a Home Assistant area and the VerdiGrow container(s)/area(s) it covers. This scopes the sensor list so the picker only shows sensors from linked HA areas.",
        "data": {
          "ha_area": "Home Assistant area",
          "targets": "VerdiGrow targets (one or more)"
        }
      }
    },
    "abort": {
      "cannot_connect": "Could not reach VerdiGrow to load containers/areas.",
      "no_mappings": "There are no mappings to remove yet.",
      "map_area_first": "Link an HA area to VerdiGrow first (so the sensor list can be scoped).",
      "no_entities_in_areas": "No sensors found in the linked HA area(s)."
    }
  }
}